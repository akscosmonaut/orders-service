services:
  postgres:
    image: postgres
    restart: always
    environment:
      POSTGRES_DB: orders-service
      POSTGRES_USER: orders-service
      POSTGRES_PASSWORD: orders-service
    ports:
      - '5432:5432'
